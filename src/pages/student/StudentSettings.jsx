import AccountSettings from "../../components/account/AccountSettings";

export default function StudentSettings() {
  return (
    <AccountSettings
      title="Student Settings"
      subtitle="Update profile, password, and notifications"
    />
  );
}
